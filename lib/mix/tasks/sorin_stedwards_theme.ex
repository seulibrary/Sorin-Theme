defmodule Mix.Tasks.SorinStedwardsTheme do
  use Mix.Task
  require Mix.Generator

  @shortdoc "Create and migrate necessary files for the SorinTheme."
  def run(_args) do
    IO.puts "SorinTheme Setup"
  end
end